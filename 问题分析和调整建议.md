# Swap 报价异常问题分析

## 问题现象

本地页面：<http://localhost:3000/zh/swap>

相关代码目录：

```text
/Users/zhujie/workspace/zood/my-next-app/app/[local]/swap
```

目前存在两个主要现象：

1. 输入 token0、预估 token1 时，请求可以正常发出，但无论输入多少，返回结果都是 `0`。
2. 输入 token1、预估 token0 时，请求没有发出，浏览器报错：

   ```text
   TypeError: Cannot convert undefined to a BigInt
   ```

## 结论

问题不是 RPC 或用户输入值导致的，而是前端报价逻辑中存在以下四个问题：

1. 最大流动性池不一定能在当前方向成交。
2. `sqrtPriceLimitX96` 仅相对当前价格偏移 `1n`，导致输出被取整为零。
3. `quoteExactOutput` 参数对象的字段名写错，导致 viem 编码 `undefined`。
4. Exact Output 场景中的 `tokenIn` 和 `tokenOut` 方向传反。

此外，报价结果还缺少 `formatUnits` 格式化。

## 1. token0 → token1 报价恒为 0

### 1.1 选择了当前方向不可成交的池

当前代码在 `SwapContent.tsx` 中根据 `liquidity` 选择流动性最大的池：

```ts
const matchedPool = candidatePools.reduce((prev, pool: PoolItem) => {
  // ...
  return prev.liquidity > pool.liquidity ? prev : pool;
}, null);
```

链上只读调用显示，当前 tokenA/tokenB 最大流动性池为 index 10，其状态为：

```text
poolIndex = 10
tickLower = 193400
tick      = 193400
```

对于 token0 → token1 方向，价格需要向下移动。该池的当前 `tick` 已经等于 `tickLower`，没有剩余的向下价格空间，所以即使池子的 `liquidity` 数值最大，也无法在这个方向继续成交。

因此，对该池输入任意较大金额，报价依然可能为 `0`。

### 1.2 `sqrtPriceLimitX96 - 1n` 过于接近当前价格

当前报价参数为：

```ts
sqrtPriceLimitX96: matchedPool.sqrtPriceX96 - 1n
```

`sqrtPriceX96` 是 Q64.96 定点数。减去 `1n` 只允许价格移动一个最小整数单位，可成交数量极小，最终计算 `amountOut` 时会被 Solidity 整数除法取整为 `0`。

对一个仍有成交空间的 index 1 池进行只读报价测试，输入 `1 token` 的结果为：

```text
sqrtPriceLimitX96 = currentSqrtPriceX96 - 1
amountOut         = 0

sqrtPriceLimitX96 = 有效的远端价格边界
amountOut         = 0.936888940892443019
```

这说明即使选中了可成交池，使用 `currentSqrtPriceX96 ± 1n` 仍然会造成报价为零。

### 建议

- 不要只根据 `liquidity` 选池。
- 至少过滤掉当前交易方向上已经到达价格边界的池。
- 更可靠的方式是对所有候选池分别报价：
  - Exact Input 选择 `amountOut` 最大的池。
  - Exact Output 选择 `amountIn` 最小的池。
- `sqrtPriceLimitX96` 应根据允许滑点计算，或者使用协议允许的方向边界；不要简单使用当前价格 `±1n`。

## 2. `undefined` 无法转换为 BigInt

`useSwapRoute.ts` 中构造 Exact Output 参数时使用了错误的字段名：

```ts
const arg = {
  tokenIn,
  tokenOut,
  indexPath: [poolIndex],
  amount: parseUnits(amountOut, 18),
  sqrtPriceLimitX96: sqrtPriceLimitX96 ?? 0n,
};
```

但是 ABI 中 `QuoteExactOutputParams` 要求的字段是：

```ts
{
  tokenIn,
  tokenOut,
  indexPath,
  amountOut,
  sqrtPriceLimitX96,
}
```

viem 使用 ABI tuple 的字段名读取对象。因为参数对象里不存在 `amountOut`，编码器实际拿到的是：

```ts
arg.amountOut === undefined
```

随后 viem 尝试把它编码成 `uint256`，等价于对 `undefined` 执行 BigInt 转换，因此抛出：

```text
TypeError: Cannot convert undefined to a BigInt
```

这个异常发生在本地 ABI 编码阶段，所以网络请求不会发出。

正确字段应为：

```ts
const arg = {
  tokenIn,
  tokenOut,
  indexPath: [poolIndex],
  amountOut: parseUnits(amountOut, 18),
  sqrtPriceLimitX96: sqrtPriceLimitX96 ?? 0n,
};
```

## 3. Exact Output 的代币方向传反

页面上方为“卖出 token0”，下方为“买入 token1”。

用户在下方输入 token1 数量时，含义是：

```text
为了得到指定数量的 token1，需要投入多少 token0？
```

因此调用 `quoteExactOutput` 时仍应传递：

```ts
tokenIn: selectedToken0Address,
tokenOut: selectedToken1Address,
```

当前代码却传成了：

```ts
tokenIn: selectedToken1Address,
tokenOut: selectedToken0Address,
```

这相当于询问“为了得到 token0，需要投入多少 token1”，与当前表单语义相反。

候选池查询也应以实际的 `tokenIn`/交易对为准，不应因为编辑了下方输入框就交换交易方向。

## 4. 报价结果缺少单位格式化

当前代码直接将 bigint 写入输入框：

```ts
form.setValue('amount1', `${result}`);
form.setValue('amount0', `${result}`);
```

合约返回的是代币最小单位。例如 18 位精度代币的：

```text
936888940892443019
```

在 UI 中应显示为：

```text
0.936888940892443019
```

因此需要按照对应输出代币的 decimals 格式化：

```ts
formatUnits(result, tokenDecimals)
```

同样，当前 `parseUnits(..., 18)` 和余额读取均固定使用 18 位精度。如果未来 token 精度不同，还需要从代币元数据或 ERC-20 `decimals()` 获取真实精度。

## 推荐修复顺序

1. 将 Exact Output 参数中的 `amount` 改为 `amountOut`。
2. 修正 Exact Output 的 `tokenIn`/`tokenOut` 方向。
3. 替换 `currentSqrtPriceX96 ± 1n` 的价格限制算法。
4. 调整选池策略，对候选池实际报价后选择最优池。
5. 使用 `formatUnits` 格式化报价结果，并使用实际 token decimals。
6. 为报价函数增加 `try/catch`、过期请求处理和加载状态，避免异步报价异常成为未处理 Promise。

## 涉及文件

- `/Users/zhujie/workspace/zood/my-next-app/app/[local]/swap/components/SwapContent.tsx`
- `/Users/zhujie/workspace/zood/my-next-app/app/[local]/swap/hooks/useSwapRoute.ts`
- `/Users/zhujie/workspace/zood/my-next-app/app/[local]/swap/hooks/abi.ts`
- `/Users/zhujie/workspace/zood/my-next-app/contracts/MetaNodeSwap/Pool.sol`
- `/Users/zhujie/workspace/zood/my-next-app/contracts/MetaNodeSwap/SwapRouter.sol`

## 验证情况

- 已在本地页面复现 token0 → token1 返回 `0`。
- 已在本地页面复现 token1 输入时的 `Cannot convert undefined to a BigInt`。
- 已通过链上只读调用验证最大流动性池处于当前交易方向的价格边界。
- 已通过链上只读调用验证 `currentSqrtPriceX96 - 1n` 会导致可成交池的结果被取整为 `0`。
- 本次仅进行诊断和只读调用，没有修改项目代码。
